module.exports = {
  command : 'status',
  aliases : [ 's' ],
  desc : 'get the currently selected plugin and other info',
  builder : (yargs) => {},
  handler : (argv) => { console.log("not yet implemented") }
}
